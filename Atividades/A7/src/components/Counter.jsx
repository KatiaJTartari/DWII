/* Esse arquivo Counter.jsx não será mais usado no projeto
import { useState } from "react";

export function Counter() {
    //imutabilidade
    const [counter, setCounter] = useState(0)

    function increment() {
        setCounter(counter + 1)
    }

    return(
        <div>
            <h2>{counter}</h2>
            <button type="button" onClick={increment}>Incrementar</button>
        </div>
    )
}
*/

/*
Site onde o prof. pegou o comando: https://github.com/pmmmwh/react-refresh-webpack-plugin
Comando para rodar no terminal: yarn add -D @pmmmwh/react-refresh-webpack-plugin react-refresh
*/